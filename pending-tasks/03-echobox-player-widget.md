# Echobox player widget

**Status:** Placeholder — currently just links out.

## Why it matters

The `#echobox-player` card on the homepage (`index.html`) is a static card
that links to `https://www.echobox.radio/`. It should be Echobox's actual
embed/player widget once they provide one, so visitors can listen without
leaving the site.

## What to do

1. Ask Echobox Radio for their official embed/widget snippet (iframe or
   JS embed).
2. Replace the contents of the `<div class="player-card" id="echobox-player">`
   block in `index.html` with it.
3. Remove the `<!-- PLACEHOLDER -->` comment above it once done.
